name: Deploy Signaling Server

on:
  push:
    branches: [ "main" ]
    paths:
      - 'server/**' # Only runs if code in your backend folder changes

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # --- Choose your deployment platform below ---

      # Example A: Deploying via SSH to a VPS / Cloud Server
      # - name: Deploy via SSH
      #   uses: appleboy/ssh-action@v1.0.3
      #   with:
      #     host: ${{ secrets.SERVER_HOST }}
      #     username: ${{ secrets.SERVER_USER }}
      #     key: ${{ secrets.SERVER_SSH_KEY }}
      #     script: |
      #       cd /path/to/your/server
      #       git pull origin main
      #       npm install
      #       pm2 restart server

      # Example B: Deploying to Render via Deploy Hook
      # - name: Trigger Render Deploy
      #   run: curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK_URL }}
